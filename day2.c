#include <stdio.h>
#include <stdlib.h>


int main()
{
	FILE *filePointer = fopen("day2_input.txt", "r");
	char ch;

	int length = 0; 
	int l;
	int w;
	int h;
	int smallestArea;
	int areaOfOne;
	int totalFeet = 0;
	
	while ((ch = fgetc(filePointer)) != EOF) 
	{
		if (ch != 'x' && ch != '\n')
	}


	return 0;
}
